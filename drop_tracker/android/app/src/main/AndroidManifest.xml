<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Reminders: notifications + exact alarms that survive reboot.
         POST_NOTIFICATIONS and VIBRATE are also declared by
         flutter_local_notifications' own bundled manifest (see its
         android/src/main/AndroidManifest.xml) and would be merged in either
         way — kept explicit here anyway since they're the two permissions
         most central to what this app does. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    <uses-permission android:name="android.permission.VIBRATE"/>
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED"/>

    <!-- Exact-alarm scheduling: flutter_local_notifications' own README
         (see its "Requesting exact alarm permission" section) documents
         SCHEDULE_EXACT_ALARM and USE_EXACT_ALARM as *alternatives*, not
         both together —
           * SCHEDULE_EXACT_ALARM: special-access permission the user grants
             through system Settings (ExactAlarmScreen owns this flow via
             permission_handler's Permission.scheduleExactAlarm). Safe for
             any app to declare.
           * USE_EXACT_ALARM: auto-granted with no user prompt, but per
             Android's own docs (developer.android.com/reference/android/
             Manifest.permission#USE_EXACT_ALARM) is intended for
             alarm-clock/calendar-category apps specifically and "could be
             subject to approval and auditing by the app store(s)" —
             Drop Tracker is a medication reminder app, not that category,
             so this was removed rather than risk Play Store rejection.
             Declaring both was also actively confusing during testing: it
             made canScheduleExactAlarms() return true via USE_EXACT_ALARM's
             auto-grant regardless of the real SCHEDULE_EXACT_ALARM state,
             masking whether the mandatory gate below was actually working.
         Do not re-add USE_EXACT_ALARM without confirming Play Store category
         eligibility first. -->
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM"/>

    <!-- Android only: native "unrestricted battery" system dialog — a
         documented, Play-Store-accepted exemption category for apps whose
         core function is scheduled, time-critical reminders. -->
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS"/>

    <application
        android:label="Drop Tracker"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:screenOrientation="portrait"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>

            <!-- Supabase OAuth (Google / Apple) browser redirect back into the
                 app. Must match SupabaseConfig.oauthRedirect and the Redirect
                 URL allow-listed in Supabase → Authentication → URL
                 Configuration. -->
            <intent-filter android:autoVerify="false">
                <action android:name="android.intent.action.VIEW"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <category android:name="android.intent.category.BROWSABLE"/>
                <data android:scheme="com.eyedropshop.droptracker" android:host="login-callback"/>
            </intent-filter>
        </activity>

        <!-- flutter_local_notifications: scheduled + reboot receivers -->
        <receiver
            android:exported="false"
            android:name="com.dexterous.flutterlocalnotifications.ScheduledNotificationReceiver" />
        <receiver
            android:exported="false"
            android:name="com.dexterous.flutterlocalnotifications.ScheduledNotificationBootReceiver">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED"/>
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED"/>
                <action android:name="android.intent.action.QUICKBOOT_POWERON" />
                <action android:name="com.htc.intent.action.QUICKBOOT_POWERON"/>
            </intent-filter>
        </receiver>

        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>

    <queries>
        <!-- Flutter engine text processing -->
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
        <!-- url_launcher: open web (privacy / terms / deletion portal) -->
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="https" />
        </intent>
        <!-- DeviceReliabilityService: package visibility (Android 11+) for
             the OEM autostart/battery-manager apps it deep-links to. Without
             these, AndroidIntent.launch() fails to resolve the target even
             when the app is actually installed. -->
        <package android:name="com.miui.securitycenter" />
        <package android:name="com.miui.powerkeeper" />
        <package android:name="com.coloros.safecenter" />
        <package android:name="com.oppo.safe" />
        <package android:name="com.iqoo.secure" />
        <package android:name="com.vivo.permissionmanager" />
        <package android:name="com.huawei.systemmanager" />
        <package android:name="com.asus.mobilemanager" />
    </queries>
</manifest>
